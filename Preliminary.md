# 病理学数据集前置知识

本页帮助你在使用本仓库的**公开病理数据集**时，快速对齐**术语、任务类型与数据层次**，便于阅读数据表、筛选任务与阅读相关论文。下文不替代系统的病理学或影像学课程。

---

## 数字病理与全切片图像（WSI）

### WSI 是什么

**WSI（Whole Slide Imaging，全玻片成像）** 常译为**全玻片图像**、**全切片影像**或**数字切片**：用扫描仪把整张玻璃病理切片以高分辨率数字化，通常拼接为单个大文件（如 `.svs`），保留从低倍宏观结构到高倍细胞细节的信息。

字母含义：**W**hole（整片）、**S**lide（玻片）、**I**maging（成像）。在 WSI 普及之前，数字化往往只能截取若干**视野（Field of View, FoV）**；WSI 则覆盖**整张切片**的连续信息。

### 视场（FoV）与「放大倍数」

**FoV（视场）** 指成像时**一次成像所覆盖的切片区域**，与物镜放大倍数、相机传感器等共同决定你在屏幕上看到的范围与分辨率。阅读数据集说明时，常会看到「20×」「40×」等，需与**像素尺寸（如 µm/像素）**一起理解，才能比较不同数据集。

---

## TCGA 与「病理图像 + 多组学」

### TCGA 简介

**TCGA（The Cancer Genome Atlas，癌症基因组图谱）** 由 NCI 与 NHGRI 等发起，对大量原发性肿瘤及配对样本做多组学刻画，覆盖多种癌症类型。它不仅是基因组资源，也包含**临床信息**与**病理全切片图像（WSI）**等，常被用于**跨模态**或**影像–组学**研究。

### 图像与「非图像特征」

病理图像提供**形态与结构**等**可直接目视或可计算**的信息；突变、表达量、甲基化等则多来自测序与芯片，通常以**表格/向量**形式存在，**不能**从单张 H&E 图「直接读出」，但可与影像在**病例维度**上关联。浏览本仓库时，「任务类型」「数据格式」等字段即是在区分这类差异。

### TCGA 中常见数据类型（示意）

| 数据类型 | 内容概要 | 常见形式示例 |
|----------|----------|----------------|
| 病理图像 | H&E 等染色的 WSI | `.svs` 等 |
| 基因组学 | 突变、拷贝数等 | `.maf`、`.seg` 等 |
| 转录组学 | 基因表达 | `.tsv` 等 |
| 表观遗传学 | 甲基化等 | 矩阵类文件 |
| 临床 | 分期、生存等 | 表格 |

---

## 病理学分支与「尺度」

### 学科划分（概念）

- **病理学（Pathology）** 大致可分为**临床病理**与**解剖病理**等；与**计算病理**、**数据集**最相关的是**解剖病理**下的**组织病理学（Histopathology）**与**细胞病理学（Cytopathology）**——前者多基于**组织切片**，后者多基于**涂片/细针**等**细胞学**标本。本仓库条目会涉及器官、切片、细胞核等不同层次。

### 从器官到亚细胞（阅读标签时的层次）

在描述数据或论文时，常见层次为：

**器官 / 解剖部位 → 组织类型 → 组织结构 → 细胞类型 → 亚细胞结构**

本站的「器官」「结构类型（如 Cell / Tissue）」等筛选，即是在不同层次上对齐任务与标签。

---

## 计算任务中的对象：细胞核、组织与分割

### 细胞核分类与组织分类

| 特点 | 细胞核分类（Nuclei Classification） | 组织分类（Tissue Classification） |
|------|--------------------------------------|-------------------------------------|
| 分析对象 | **单个**细胞核（偏微观） | **图像块或整张 WSI**（偏宏观/区域） |
| 关注点 | 核形态、大小、染色等 | 排列方式、纹理、整体细胞组成等 |
| 典型输出 | **每个核**的类别 | **每个区域/整张图**的类别 |
| 关系 | 核级结果常可作为组织级模型的**中间特征** | 组织级往往是更高层**决策或诊断支持** |

### 染色与成像方式（读数据集说明时常见）

公开数据与论文中常见写法包括：

- **Brightfield H&E**：明场 + **苏木精–伊红（H&E）染色**，最常用，核与胞质对比清晰。
- **Brightfield H-DAB 等**：明场 + 免疫组化显色（如 DAB），用于标记特定蛋白。
- **Brightfield Other / Fluorescence / Other**：其他染色或荧光、电镜等；**Unspecified** 表示元数据未写明。

> **【待补充资源 · 图片】**  
> **建议文件名**：`preliminary-staining-modalities.png`  
> **建议放置路径**（相对站点根目录）：`assets/images/preliminary/preliminary-staining-modalities.png`  
> **内容建议**：一张图汇总「明场 H&E / IHC / 荧光」等示意（可为示意图，不要求与本站数据一一对应）。  
> **添加后**：在上一小节正文末尾自行插入一行：  
> `![](assets/images/preliminary/preliminary-staining-modalities.png)`

---

## 组织与细胞：标签里的常见语义

### 组织层面常见类别（任务标签示例）

下列在**语义分割、多类分类**数据集中经常出现；具体定义以各数据集说明为准。

| 组织/区域类型 | 简要含义 | CV 标签示例（示意） |
|---------------|----------|---------------------|
| 上皮（Epithelial） | 腺上皮、鳞状上皮等 | epithelium / tumor-epi |
| 肿瘤上皮 | 异型增生、癌巢等 | tumor / carcinoma |
| 间质（Stroma） | 纤维、成纤维细胞等 | stroma |
| 炎性浸润 | 淋巴、浆细胞等聚集 | inflam / lymphoid |
| 坏死 | 结构崩解、核消失等 | necrosis |
| 血管 | 管腔、内皮等 | vessel |
| 脂肪、肌肉、神经等 | 按形态区分 | fat / muscle / nerve |
| 黏液、伪影等 | 黏液湖、刀痕、折叠等 | mucin / artifact |

### 常见细胞类型（举例）

数据集若做**细胞检测/分类**，常见类别包括：肿瘤细胞、上皮细胞、淋巴细胞、巨噬细胞、浆细胞、成纤维细胞、内皮细胞、中性粒细胞、嗜酸性粒细胞等。**坏死碎片、红细胞**等是否单独成类，依数据集定义而定；阅读时请对照各数据集的**类别表与论文**。

---

## 数据集如何划分任务与「对象」

### 解剖部位、结构层次与对象类型

部分综述或基准工作会从三个维度描述病理数据或标注：

1. **解剖区域 / 器官**（如结直肠、前列腺、乳腺）  
2. **结构层次**：细胞核、细胞、组织/腺体等  
3. **对象类型**：具体类别列表（不同论文从十余类到数十类不等）

本站「器官」「结构类型」「任务类型」等筛选，可与上述思路对照，但**具体类别数以各数据集为准**。

### 「腺体」等结构指什么

**腺体（gland）** 在腺上皮来源器官（如结肠、前列腺）的切片中，常指**腺腔及其周围上皮**形成的结构；在分割任务中常与「上皮」「肿瘤」「间质」等并列，表示**一种需要勾画或分类的结构单元**。不同数据集对「腺体实例分割」与「语义类别」的定义可能不同，需读**原文与标注说明**。

### 分割任务的大致分法

- **细胞核分割**：逐核轮廓，常不区分类别或单独做核型分类。  
- **组织结构分割**：上皮/间质/肿瘤区域、腺管结构等**区域级**类别。  
- **多类别语义分割**：在同一张图上同时为**多类结构**赋语义标签（如肿瘤、基质、坏死、背景等）。

其中**坏死、基质**等通常仍属于**生物学上有意义的「组织/区域」语义**；是否单独成类取决于**标注协议与任务设计**，而不是「算不算组织」的绝对二元问题。

> **【待补充资源 · 图片】**  
> **建议文件名**：`preliminary-task-taxonomy.png`  
> **建议路径**：`assets/images/preliminary/preliminary-task-taxonomy.png`  
> **内容建议**：示意图展示「核分割 / 组织分割 / 多类语义分割」或与器官、层次、对象类型相关的**概念图**（可引用公开教程/综述中的示意风格，注意版权与引用）。  
> **添加后**：在本小节适当位置插入：  
> `![](assets/images/preliminary/preliminary-task-taxonomy.png)`

---

## 延伸阅读（入门向）

### 综述与检索关键词

下列方向有助于**系统了解数据集与任务**（具体篇名与版本请自行检索最新文献）：

- 计算病理**基础模型**综述：大规模数据、自监督与评测任务。  
- **数字病理分割**相关综述：任务定义、挑战与指标。  
- 特定癌种或器官（如结直肠、乳腺）的深度学习病理图像分析综述。

可在 arXiv、PubMed、Google Scholar 等使用例如：`digital pathology dataset survey`、`computational pathology review`、`whole slide image segmentation review`、`pathology foundation model` 等关键词组合。

### 书目（教材与参考书）

- *Robbins and Cotran Pathologic Basis of Disease* — 病理学通用教材  
- *Histology for Pathologists* — 组织学进阶  
- *WHO Classification of Tumors*（各卷）— 肿瘤分类与诊断标准  

---

# ViPP 仓库：PathSeg 统一数据集结构

**ViPP** 相关预处理管线（如 **PathSeg**）将多种原始公开格式规整为**同一套目录与字段约定**，便于训练、评测与溯源。下列为**输出根目录**（示例名 `[PathSeg]`）下的**标准布局**与核心文件语义；若你使用本站 `datasets/` 条目对照「统一格式」字段，可与下文对齐。

## 标准化目录布局

```text
[PathSeg] (输出根目录)
├── images_png/               # 图像库：裁剪后的子图 (PNG, RGB)
│   ├── 00000000.png
│   └── ...
├── nuclei_labels_pkl/        # 核级标注库：每个细胞核的实例掩码 (Pickle)
│   ├── 00000000.pkl
│   └── ...
├── metadata.csv              # 溯源索引表：空间位置与生物学属性
└── nuclei_counts.csv         # 统计表：各类细胞核数量汇总
```

## `images_png/`（图像子块）

- **内容**：从原始大图裁切的 **256×256** 像素 RGB 子图（PNG）。
- **过滤**：**空值过滤**——仅保留含目标细胞核的有效区域，剔除纯背景或无标注意义的空块。

## `nuclei_labels_pkl/`（核实例标注）

每个 `.pkl` 与同名 PNG **一一对应**，通常为一个字典；其中描述**多个细胞核实例**的核心字段如下（具体键名以实现代码为准）。

- **`cell`（`List[Dict]`）**：实例列表。每个元素为一个核的字典，常见键包括：
  - **`src_class_name`**（`str`）：源数据集的原始类别名；不同来源数据集命名规则不同。以 **Lizard** 为例，六类为：`neutrophil`、`epithelial`、`lymphocyte`、`plasma`、`eosinophil`、`connective`。
  - **`tgt_class_name`**（`str`）：映射后的**目标类别名**；由你在统一标签体系中的 **`src` → `tgt` 映射表**自行定义。
  - **`bimask`**：`uint8` 矩阵，形状 **256×256**，表示**该核**的二值（或实例）掩膜。
  - **`Magnification`**、**`organ`**：自 `metadata` 等处同步的元数据副本（放大倍数、器官等）。
  - **`tissue`**（`List`）：预留字段，可为组织级补充信息。

## `metadata.csv`（子图溯源与空间索引）

记录每张裁剪子图的标识、来源与在原图中的位置，便于还原到 WSI 或原始大图坐标系。

| 字段 | 含义 |
|------|------|
| `img_id` | 8 位唯一 ID；**读入时按字符串**处理（如 `"00000000"`），避免前导零丢失。 |
| `Magnification` | 放大倍数（如 20×、40×）。 |
| `organ` | 来源器官或解剖部位。 |
| `height`、`width` | 子图尺寸（通常为 256）。 |
| `source` | 溯源字符串，常见形式为 `"{dataset_name}_原始图像文件名"`（含原始文件后缀）。 |
| `crop_top`、`crop_left` | 子图在**原图**中的左上角像素坐标，用于空间对齐与回贴。 |
| `split` | 划分标识，如 `train` / `eval`。 |

## `nuclei_counts.csv`（类别分布统计）

用于快速查看每张图内各类细胞核数量，便于采样与类别平衡。

| 字段 | 含义 |
|------|------|
| `img_id` | 与上表一致，8 位 ID，**按字符串**读取。 |
| **目标类别列** | 每个 **目标类别名** 占一列，值为该图中该类核的**个数**。 |
| `splits` | 划分标识（与 `metadata.csv` 中 `split` 对应）。 |

> **说明**：`metadata.csv` 与 `nuclei_counts.csv` 的列名若随管线版本微调，以 ViPP / PathSeg 仓库内**最新 schema 或示例脚本**为准；上表描述的是**设计意图**与常见字段。

---

*文档随仓库维护更新；数据集条目以 `datasets/` 与各数据集的说明为准。*
